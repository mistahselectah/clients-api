export enum ERole {
  ADMIN = 'ADMIN',
  USER = 'USER',
}

export enum EResource {
  CLIENTS = 'CLIENTS',
}

export enum EAction {
  CREATE = 'CREATE',
  UPDATE = 'UPDATE',
  LIST = 'LIST',
  DELETE = 'DELETE',
  GET_TOTAL_AMOUNT = 'GET_TOTAL_AMOUNT',
}
